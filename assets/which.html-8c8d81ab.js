import{_ as e}from"./plugin-vue_export-helper-c27b6911.js";import{o as a,c,e as o}from"./app-be253e4c.js";const s={},n=o(`<h1 id="which命令" tabindex="-1"><a class="header-anchor" href="#which命令" aria-hidden="true">#</a> which命令</h1><p><code>which</code>命令用于标识在终端提示符下键入可执行文件名称或命令时执行的给定可执行文件的位置，该命令在<code>PATH</code>环境变量中列出的目录中搜索作为参数指定的可执行文件。</p><h2 id="语法" tabindex="-1"><a class="header-anchor" href="#语法" aria-hidden="true">#</a> 语法</h2><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token function">which</span> <span class="token punctuation">[</span>options<span class="token punctuation">]</span>  COMMAND
</code></pre></div><h2 id="参数" tabindex="-1"><a class="header-anchor" href="#参数" aria-hidden="true">#</a> 参数</h2><ul><li><code>--version, -[vV]</code>: 输出版本信息。</li><li><code>--help</code>: 输出帮助信息。</li><li><code>--skip-dot</code>: 跳过<code>PATH</code>中以<code>.</code>开头的目录。</li><li><code>--skip-tilde</code>: 跳过<code>PATH</code>中以<code>~</code>开头的目录。</li><li><code>--show-dot</code>: 不要在输出中将<code>.</code>扩展到当前目录。</li><li><code>--show-tilde</code>: 为非<code>root</code>用户输出<code>HOME</code>目录的波浪号。</li><li><code>--tty-only</code>: 如果不在<code>tty</code>上，则停止处理右侧的选项。</li><li><code>--all, -a</code>: 打印<code>PATH</code>中的所有匹配项，而不仅仅是第一个。</li><li><code>--read-alias, -i</code>: 从标准输入中读取别名列表。</li><li><code>--skip-alias</code>: 忽略选项<code>--read-alias</code>，不要读标准输入。</li><li><code>--read-functions</code>: 从标准输入读取<code>shell</code>函数。</li><li><code>--skip-functions</code>: 忽略选项<code>--read-functions</code>，不要读标准输入。</li></ul><h2 id="示例" tabindex="-1"><a class="header-anchor" href="#示例" aria-hidden="true">#</a> 示例</h2><p>查看指令<code>bash</code>的绝对路径。</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token function">which</span> <span class="token function">bash</span>
<span class="token comment"># /usr/bin/bash</span>
</code></pre></div><p>查看多个命令的绝对路径。</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token function">which</span> <span class="token function">ping</span> <span class="token function">touch</span>
<span class="token comment"># /usr/bin/ping</span>
<span class="token comment"># /usr/bin/touch</span>
</code></pre></div><p>输出所有在环境变量中的匹配项的绝对路径。</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token function">which</span> <span class="token parameter variable">-a</span> python
<span class="token comment"># ~/anaconda3/bin/python</span>
<span class="token comment"># /usr/bin/python</span>
</code></pre></div>`,13),d=[n];function i(t,l){return a(),c("div",null,d)}const r=e(s,[["render",i],["__file","which.html.vue"]]);export{r as default};
