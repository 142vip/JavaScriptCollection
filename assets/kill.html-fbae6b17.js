import{_ as e}from"./plugin-vue_export-helper-c27b6911.js";import{o as c,c as o,e as d}from"./app-be253e4c.js";const a={},s=d(`<h1 id="kill命令" tabindex="-1"><a class="header-anchor" href="#kill命令" aria-hidden="true">#</a> kill命令</h1><p><code>kill</code>命令向指定的<code>pid</code>进程发送信号，如果不指定要发送的<code>signal</code>信号，则默认情况下<code>signal</code>是<code>SIGTERM</code>，它会终止进程，要列出所有可用信号，可以使用<code>-l</code>选项获取<code>Linux</code>信号列表，经常使用的信号包括<code>HUP</code>、<code>INT</code>、<code>KILL</code>、<code>STOP</code>、<code>CONT</code>和<code>0</code>，可以通过三种方式指定信号: 按数字例如<code>-9</code>，带有<code>SIG</code>前缀例如<code>-SIGKILL</code>，不带<code>SIG</code>前缀例如<code>-KILL</code>。负<code>PID</code>值用于指示过程组<code>ID</code>，如果传递了进程组<code>ID</code>，则该组中的所有进程都将接收到该信号，<code>PID</code>为<code>-1</code>是特殊的，其指示除两个以外的所有进程，<code>kill</code>进程本身和<code>init</code>即<code>PID 1</code>，其是系统上所有进程的父进程，将<code>-1</code>指定为目标会将信号发送到除这两个以外的所有进程。</p><h2 id="语法" tabindex="-1"><a class="header-anchor" href="#语法" aria-hidden="true">#</a> 语法</h2><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token function">kill</span> pid <span class="token punctuation">..</span>.
<span class="token function">kill</span> <span class="token punctuation">{</span>-signal <span class="token operator">|</span> <span class="token parameter variable">-s</span> signal<span class="token punctuation">}</span> pid <span class="token punctuation">..</span>.
<span class="token function">kill</span> <span class="token punctuation">{</span>-l <span class="token operator">|</span> --list<span class="token punctuation">[</span><span class="token operator">=</span>signal<span class="token punctuation">]</span> <span class="token operator">|</span> <span class="token parameter variable">-L</span> <span class="token operator">|</span> --table<span class="token punctuation">}</span>
</code></pre></div><h2 id="参数" tabindex="-1"><a class="header-anchor" href="#参数" aria-hidden="true">#</a> 参数</h2><ul><li><code>-signal, -s signal</code>: 要发送的信号的名称、缩写名或编号，前面有破折号，例如<code>-SIGTERM</code>、<code>-TERM</code>、<code>-15</code>等，要查看可用信号的列表可以使用<code>kill -l</code>。</li><li><code>pid</code>: 数字进程<code>ID</code>，如果不确定某个进程的<code>PID</code>是什么，可以使用<code>ps</code>命令将其列出例如<code>ps -aux</code>，通常会配合管道与<code>grep</code>使用例如查看<code>nginx</code>相关的进程<code>ps -aux | grep nginx</code>。</li><li><code>-l, --list[=signal]</code>: 列出可用的信号名称，用<code>-l</code>或<code>--list</code>列出所有信号名称，使用<code>--list = signal</code>，可将数字转换为其信号名称。</li><li><code>-L, --table</code>: 在表格中列出可用的信号名称和编号。</li></ul><h2 id="信号" tabindex="-1"><a class="header-anchor" href="#信号" aria-hidden="true">#</a> 信号</h2><p>下列的某些信号是不能使用的，因为系统有可能不支持这些信号，或者这些信号已中断，详情可以实际查阅系统的<code>kill -l</code>命令。每个系统都有信号<code>1</code>、<code>3</code>、<code>9</code>和<code>15</code>，这些是最常见的信号。终止信号不仅用于关闭锁定的应用程序，还可以阻止软件执行不允许的任务，这意味着其中一些终止信号是安全的一部分，另外<code>kill</code>命令不仅停止<code>/</code>终止进程，而且还暂停，继续和重新启动进程。</p><div class="language-text" data-ext="text"><pre class="language-text"><code> 1) SIGHUP       2) SIGINT       3) SIGQUIT      4) SIGILL       5) SIGTRAP
 6) SIGABRT      7) SIGBUS       8) SIGFPE       9) SIGKILL     10) SIGUSR1
11) SIGSEGV     12) SIGUSR2     13) SIGPIPE     14) SIGALRM     15) SIGTERM
16) SIGSTKFLT   17) SIGCHLD     18) SIGCONT     19) SIGSTOP     20) SIGTSTP
21) SIGTTIN     22) SIGTTOU     23) SIGURG      24) SIGXCPU     25) SIGXFSZ
26) SIGVTALRM   27) SIGPROF     28) SIGWINCH    29) SIGIO       30) SIGPWR
31) SIGSYS      34) SIGRTMIN    35) SIGRTMIN+1  36) SIGRTMIN+2  37) SIGRTMIN+3
38) SIGRTMIN+4  39) SIGRTMIN+5  40) SIGRTMIN+6  41) SIGRTMIN+7  42) SIGRTMIN+8
43) SIGRTMIN+9  44) SIGRTMIN+10 45) SIGRTMIN+11 46) SIGRTMIN+12 47) SIGRTMIN+13
48) SIGRTMIN+14 49) SIGRTMIN+15 50) SIGRTMAX-14 51) SIGRTMAX-13 52) SIGRTMAX-12
53) SIGRTMAX-11 54) SIGRTMAX-10 55) SIGRTMAX-9  56) SIGRTMAX-8  57) SIGRTMAX-7
58) SIGRTMAX-6  59) SIGRTMAX-5  60) SIGRTMAX-4  61) SIGRTMAX-3  62) SIGRTMAX-2
63) SIGRTMAX-1  64) SIGRTMAX
</code></pre></div><ul><li><code>SIGHUP</code>: <code>SIGHUP</code>信号用于断开进程与父进程的连接，这也可以用于重新启动进程，这对于内存泄漏的守护程序很有用。</li><li><code>SIGINT</code>: 该信号与按<code>Ctrl+C</code>相同，在某些系统上<code>delete + break</code>会向进程发送相同的信号，该进程被中断并停止，但是该进程自身可以忽略此信号。</li><li><code>SIGQUIT</code>: 这类似于<code>SIGINT</code>，但由于<code>QUIT</code>字符通常是由<code>Ctrl+\\</code>来控制，进程在收到<code>SIGQUIT</code>退出时会产生<code>core</code>文件, 在这个意义上类似于一个程序错误信号。</li><li><code>SIGILL</code>: 当一个进程执行一个错误的、禁止的或未知的功能时，系统向该进程发送<code>SIGILL</code>信号，这是代表非法操作的信号。</li><li><code>SIGTRAP</code>: 此信号用于调试目的，当某个进程执行了某个操作或满足了调试器正在等待的条件时，此信号将被发送到该进程。</li><li><code>SIGABRT</code>: 该终止信号是中止信号,通常进程会在自身上发出此终止信号。</li><li><code>SIGBUS</code>: 当一个进程被发送<code>SIGBUS</code>信号时，是因为该进程导致了一个总线错误，通常这些总线错误是由于进程试图使用假物理地址或进程的内存对齐设置不正确造成的。</li><li><code>SIGFPE</code>: 被零除的进程使用<code>SIGFPE</code>终止。</li><li><code>SIGKILL</code>: <code>SIGKILL</code>信号强制进程立即停止执行，程序不能忽略该信号，此过程也无法清除。</li><li><code>SIGUSR1</code>: 这表示用户定义的条件，用户可以通过在<code>sigusr1.c</code>中编程命令来设置此信号。</li><li><code>SIGSEGV</code>: 当应用程序有分段冲突时，这个信号被发送到进程。</li><li><code>SIGUSR2</code>: 这表示用户定义的条件。</li><li><code>SIGPIPE</code>: 当进程尝试写入缺少与读取器连接的一端的管道时，此信号将发送到进程，读取器是在管道末端读取数据的过程。</li><li><code>SIGALRM</code>: 当实时或时钟计时器到期时，将发送<code>SIGALRM</code>。</li><li><code>SIGTERM</code>: 该信号请求进程停止运行，该信号可以被程序自身忽略，该过程有时间正常关闭，当程序正常关闭时，这意味着它有时间保存进度并释放资源，换句话说即它不被迫停止。</li><li><code>SIGCHLD</code>: 当父进程丢失其子进程时，将向父进程发送<code>SIGCHLD</code>信号，这将清理子进程使用的资源，子进程指的是由另一个称为父进程的进程启动的进程。</li><li><code>SIGCONT</code>: 要使进程在被<code>SIGTSTP</code>或<code>SIGSTOP</code>信号暂停后继续执行，需要将<code>SIGCONT</code>信号发送到已暂停的进程，这是<code>CONTiNUE SIGNAL</code>，该信号对<code>Unix</code>作业控制执行后台任务很有帮助。</li><li><code>SIGSTOP</code>: 该信号使操作系统暂停进程的执行，进程自身不能忽略该信号。</li><li><code>SIGTSTP</code>: 这个信号类似于按下<code>Ctrl+Z</code>，它向包含进程的终端发出请求，请求进程暂时停止，进程自身可以忽略该信号。</li><li><code>SIGTTIN</code>: 当进程试图从<code>tty</code>终端读取数据时，进程接收到这个信号。</li><li><code>SIGTTOU</code>: 当某个进程尝试向<code>tty</code>终端进行写操作时，该进程将收到此信号。</li><li><code>SIGURG</code>: 当进程中有紧急数据要读取或数据非常大时，<code>SIGURG</code>信号将发送到该进程。</li><li><code>SIGXCPU</code>: 当某个进程在分配的时间之后使用<code>CPU</code>时，系统将向该进程发送该信号，<code>SIGXCPU</code>的行为就像一个警告，该进程有时间保存进度(如果可能)并在系统使用<code>SIGKILL</code>终止该进程之前将其关闭。</li><li><code>SIGXFSZ</code>: 文件系统对文件的大小有限制，当程序尝试违反此限制时，系统将发送该过程<code>SIGXFSZ</code>信号。</li><li><code>SIGVTALRM</code>: <code>SIGVTALRM</code>在进程使用的<code>CPU</code>时间结束时发送。</li><li><code>SIGPROF</code>: 当进程以及由系统代表进程使用的<code>CPU</code>时间过去时，将发送<code>SIGPROF</code>信号。</li><li><code>SIGWINCH</code>: 当进程在更改其大小的终端中时，该进程将接收此信号。</li><li><code>SIGIO</code>: <code>SIGPOLL</code>的别名或至少表现得很像<code>SIGPOLL</code>。</li><li><code>SIGPWR</code>: 电源故障将导致系统将该信号发送到进程，如果系统仍在运行。</li><li><code>SIGSYS</code>: 为系统调用提供无效参数的进程将接收此信号。</li><li><code>SIGRTMIN*</code>: 这是一组在系统之间变化的信号，它们被标记为<code>SIGRTMIN + 1</code>、<code>SIGRTMIN + 2</code>、<code>SIGRTMIN + 3</code>、<code>...</code>，通常最多<code>15</code>个，这些是用户定义的信号，它们必须在<code>Linux</code>内核的源代码中进行编程。</li><li><code>SIGRTMAX*</code>: 这是一组在系统之间变化的信号，它们被标记为<code>SIGRTMAX-1</code>、<code>SIGRTMAX-2</code>、<code>SIGRTMAX-3</code>、<code>...</code>，通常最多<code>14</code>个，这些是用户定义的信号，它们必须在<code>Linux</code>内核的源代码中进行编程。</li><li><code>SIGEMT</code>: 指示一个实现定义的硬件故障。</li><li><code>SIGINFO</code>: 终端有时可能会向进程发送状态请求，发生这种情况时，进程也将收到此信号。</li><li><code>SIGLOST</code>: 试图访问锁定文件的进程将收到此信号。</li><li><code>SIGPOLL</code>: 当进程引起异步<code>I/O</code>事件时，会向该进程发送<code>SIGPOLL</code>信号。</li></ul><h2 id="示例" tabindex="-1"><a class="header-anchor" href="#示例" aria-hidden="true">#</a> 示例</h2><p>要显示所有可用信号，查看信号的详细信息可以使用<code>man</code>命令，例如信号<code>7 SIGBUS</code>即使用<code>man 7 signal</code>。</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token function">kill</span> <span class="token parameter variable">-l</span>
</code></pre></div><p>在表格中列出可用的信号名称和编号。</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token function">kill</span> <span class="token parameter variable">-L</span>
</code></pre></div><p>终端断线，重新加载配置文件，平滑重启。</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token function">kill</span> <span class="token parameter variable">-1</span> <span class="token number">111</span>
</code></pre></div><p>通知进程关闭，使进程自行关闭，这个关闭是安全、干净地退出，如果<code>kill</code>命令后直接加进程的<code>pid</code>号默认选项为<code>-15</code>，这个信号是可以被进程自身忽略并继续执行自身的，即该信号是可以被阻塞和忽略的。</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token function">kill</span> <span class="token parameter variable">-15</span> <span class="token number">111</span>
</code></pre></div><p>强行中断当前程序的执行，类似<code>Ctrl+C</code>按键用以结束进程的结果，该信号是可以被阻塞和忽略的。</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token function">kill</span> <span class="token parameter variable">-2</span> <span class="token number">111</span>
</code></pre></div><p>退出进程，类似<code>Ctrl+\\</code>按键用以结束进程的结果，该信号是可以被阻塞和忽略的。</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token function">kill</span> <span class="token parameter variable">-3</span> <span class="token number">111</span>
</code></pre></div><p>强行关闭进程，<code>-9</code>信号是无条件终止，这个信号不能被捕获或忽略，同时接收这个信号的进程在收到这个信号时不能执行任何清理，该信号是不可以被阻塞和忽略的，当然通常是不建议使用<code>kill -9</code>的，应该尝试使用<code>-15</code>以及<code>-2</code>给目标进程一个清理自身资源工作的机会，不要用收割机来修剪花盆里的花。</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token function">kill</span> <span class="token parameter variable">-9</span> <span class="token number">111</span>
</code></pre></div><p>暂停进程，对于暂停信号进程自身不能忽略该信号。</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token function">kill</span> <span class="token parameter variable">-19</span> <span class="token number">111</span> 
</code></pre></div><p>激活进程，对于激活信号必须发送到已暂停的进程才有效。</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token function">kill</span> <span class="token parameter variable">-18</span> <span class="token number">111</span> 
</code></pre></div>`,29),l=[s];function n(i,I){return c(),o("div",null,l)}const S=e(a,[["render",n],["__file","kill.html.vue"]]);export{S as default};
