import{_ as e}from"./plugin-vue_export-helper-c27b6911.js";import{o as a,c,e as o}from"./app-be253e4c.js";const d={},s=o(`<h1 id="cat命令" tabindex="-1"><a class="header-anchor" href="#cat命令" aria-hidden="true">#</a> cat命令</h1><p><code>cat</code>命令属于文件管理，用于连接文件并打印到标准输出设备上，<code>cat</code>经常用来显示文件的内容，注意，当文件较大时，文本在屏幕上迅速闪过，会出现滚屏现象，此时往往看不清所显示的内容，为了控制滚屏，可以按<code>Ctrl+S</code>键停止滚屏，按<code>Ctrl+Q</code>键可以恢复滚屏，此外可以用<code>more</code>等命令进行读文件并分页显示。</p><h2 id="语法" tabindex="-1"><a class="header-anchor" href="#语法" aria-hidden="true">#</a> 语法</h2><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token function">cat</span> <span class="token punctuation">[</span>-AbeEnstTuv<span class="token punctuation">]</span> <span class="token punctuation">[</span>--help<span class="token punctuation">]</span> <span class="token punctuation">[</span>--version<span class="token punctuation">]</span> fileName
</code></pre></div><h2 id="参数" tabindex="-1"><a class="header-anchor" href="#参数" aria-hidden="true">#</a> 参数</h2><ul><li><code>-n</code>或<code>--number</code>: 由<code>1</code>开始对所有输出的行数编号。</li><li><code>-b</code>或<code>--number-nonblank</code>: 和<code>-n</code>相似，只不过对于空白行不编号。</li><li><code>-s</code>或<code>--squeeze-blank</code>: 当遇到有连续两行以上的空白行，就代换为一行的空白行。</li><li><code>-v</code>或<code>--show-nonprinting</code>: 使用<code>^</code>和<code>M-</code>符号，除了<code>LFD</code>和<code>TAB</code>之外。</li><li><code>-E</code>或<code>--show-ends</code>: 在每行结束处显示<code>$</code>。</li><li><code>-T</code>或<code>--show-tabs</code>: 将<code>TAB</code>字符显示为<code>^I</code>。</li><li><code>-A</code>或<code>--show-all</code>: 等价于<code>-vET</code>。</li><li><code>-e</code>: 等价于<code>-vE</code>选项。</li><li><code>-t</code>: 等价于<code>-vT</code>选项。</li></ul><h2 id="示例" tabindex="-1"><a class="header-anchor" href="#示例" aria-hidden="true">#</a> 示例</h2><p>使用<code>cat</code>命令创建一个文件，输入文件信息后按<code>Ctrl+D</code>输出<code>EOF</code>标识后结束输入。</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token function">cat</span> <span class="token operator">&gt;</span> file.txt
</code></pre></div><p>输出<code>file.txt</code>文件中的内容。</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token function">cat</span> file.txt
</code></pre></div><p>同时输出<code>file.txt</code>与<code>file2.txt</code>文件中的内容。</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token function">cat</span> file.txt file2.txt
</code></pre></div><p>把<code>file.txt</code>文件的内容加上行号后追加到<code>file2.txt</code>文件中。</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token function">cat</span> <span class="token parameter variable">-n</span> file.txt <span class="token operator">&gt;&gt;</span> file2.txt
</code></pre></div><p>清空<code>file2.txt</code>文件，<code>/dev/null</code>称为空设备，是一个特殊的设备文件，其会丢弃一切写入其中的数据，但报告写入操作成功，读取它则会立即得到一个<code>EOF</code>。</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token function">cat</span> /dev/null <span class="token operator">&gt;</span> file2.txt
</code></pre></div><p>将<code>file.txt</code>与<code>file2.txt</code>文件内容合并输出到<code>file3.txt</code>。</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token function">cat</span> file.txt file2.txt <span class="token operator">&gt;</span> file3.txt
</code></pre></div>`,19),t=[s];function n(l,i){return a(),c("div",null,t)}const h=e(d,[["render",n],["__file","cat.html.vue"]]);export{h as default};
