import{_ as e}from"./plugin-vue_export-helper-c27b6911.js";import{o as c,c as o,e as d}from"./app-be253e4c.js";const s={},a=d(`<h1 id="scp命令" tabindex="-1"><a class="header-anchor" href="#scp命令" aria-hidden="true">#</a> scp命令</h1><p><code>scp</code>命令用于<code>Linux</code>之间复制文件和目录，<code>scp</code>是<code>secure copy</code>的缩写，是<code>Linux</code>系统下基于<code>ssh</code>登陆进行安全的远程文件拷贝命令，使用<code>scp</code>可以实现从本地系统到远程系统、从远程系统到本地系统、在本地系统的两个远程系统之间的复制传输。<code>scp</code>是加密的，<code>rcp</code>是不加密的，可以认为<code>scp</code>是<code>rcp</code>的加强版。</p><h2 id="语法" tabindex="-1"><a class="header-anchor" href="#语法" aria-hidden="true">#</a> 语法</h2><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token function">scp</span> <span class="token punctuation">[</span>OPTION<span class="token punctuation">]</span> <span class="token punctuation">[</span>user@<span class="token punctuation">]</span>SRC_HOST:<span class="token punctuation">]</span>file1 <span class="token punctuation">[</span>user@<span class="token punctuation">]</span>DEST_HOST:<span class="token punctuation">]</span>file2
</code></pre></div><h2 id="参数" tabindex="-1"><a class="header-anchor" href="#参数" aria-hidden="true">#</a> 参数</h2><ul><li><code>-1</code>: 强制<code>scp</code>使用协议<code>1</code>，这是一个较旧的协议。</li><li><code>-2</code>: 强制<code>scp</code>使用协议<code>2</code>，这是一个较旧的协议。</li><li><code>-3</code>: 两个远程主机之间的副本通过本地主机传输，如果没有此选项，数据将直接在两个远程主机之间复制，此选项还禁用进度表。</li><li><code>-4</code>: 强制<code>scp</code>仅使用<code>IPv4</code>地址。</li><li><code>-6</code>: 强制<code>scp</code>仅使用<code>IPv6</code>地址。</li><li><code>-B</code>: 使用批处理模式，无需任何交互式键盘输入即可运行，这意味着<code>scp</code>无法通过要求用户输入密码来验证会话，此时需要使用密钥进行身份验证。</li><li><code>-C</code>: 启用压缩，该压缩将<code>-C</code>标志传递给<code>ssh</code>以启用加密连接的压缩。</li><li><code>-c cipher</code>: 选择用于加密数据传输的密码，此选项直接传递给<code>ssh</code>。</li><li><code>-F ssh_config</code>: 为<code>ssh</code>指定每个用户的备用配置文件，此选项直接传递给<code>ssh</code>。</li><li><code>-i identity_file</code>: 选择读取<code>RSA</code>身份验证的身份(私钥)的文件，这个选项直接传递给<code>ssh</code>。</li><li><code>-l limit</code>: 限制使用的带宽，以<code>Kbit/s</code>为单位。</li><li><code>-o ssh_option</code>: 可用于以<code>ssh_config</code>中使用的格式将选项传递给<code>ssh</code>，例如<code>AddressFamily</code>、<code>BatchMode</code>、<code>BindAddress</code>等等，这对于指定没有单独的<code>scp</code>命令标志的选项非常有用。</li><li><code>-P port</code>:指定远程主机上要连接到的端口，注意这个选项是用大写字母<code>P</code>写的，因为<code>-p</code>已经被保留了，用于保存<code>rcp</code>中文件的时间和模式。</li><li><code>-p</code>: 保留原始文件的修改时间、访问时间和模式。</li><li><code>-q</code>: 禁用进度表。</li><li><code>-r</code>: 递归地复制整个目录。</li><li><code>-S program</code>: 用于加密连接的程序名称，程序必须能够解析<code>ssh</code>选项。</li><li><code>-v</code>: 详细模式，使<code>scp</code>和<code>ssh</code>打印有关其进度的调试消息，这有助于调试连接，身份验证和配置问题。</li></ul><h2 id="示例" tabindex="-1"><a class="header-anchor" href="#示例" aria-hidden="true">#</a> 示例</h2><p>将<code>file.txt</code>传输到远程主机。</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token function">scp</span> file.txt root@1.1.1.1:/tmp
<span class="token comment"># file.txt                      100%    0     0.0KB/s   00:00</span>
</code></pre></div><p>使用私钥将<code>file.txt</code>传输到远程主机。</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token function">scp</span> <span class="token parameter variable">-i</span> ./v file.txt root@1.1.1.1:/tmp
<span class="token comment"># file.txt                      100%    0     0.0KB/s   00:00</span>
</code></pre></div><p>将远程目录中<code>file.txt</code>文件传输到本地，使用私钥验证身份。</p><div class="language-text" data-ext="text"><pre class="language-text"><code>scp -i ./v root@1.1.1.1:/tmp/file.txt ./
# file.txt                      100%    0     0.0KB/s   00:00
</code></pre></div>`,13),n=[a];function t(i,l){return c(),o("div",null,n)}const h=e(s,[["render",t],["__file","scp.html.vue"]]);export{h as default};
