import{_ as c}from"./plugin-vue_export-helper-c27b6911.js";import{r as p,o as l,c as u,a as s,b as n,d as t,w as r,e as a}from"./app-be253e4c.js";const i="/JavaScriptCollection/assets/sequelize-query-19b6ad63.png",k="/JavaScriptCollection/assets/query-options-type-a656861d.png",d="/JavaScriptCollection/assets/findAll-api-64b4e0f7.png",g="/JavaScriptCollection/assets/apis-findOrCreate-0cb8fe21.png",y="/JavaScriptCollection/assets/apis-findAndCountAll-99235d91.png",f={},m=a(`<h1 id="db查询" tabindex="-1"><a class="header-anchor" href="#db查询" aria-hidden="true">#</a> DB查询</h1><p><code>Sequelize</code>提供了各种封装好的<code>API</code>方法来支持数据库查询操作，在介绍查询操作前， 可以先向表中插入一些数据，例如：</p><div class="language-javascript" data-ext="js"><pre class="language-javascript"><code><span class="token comment">// 向表中插入一行数据</span>
<span class="token keyword">const</span> user <span class="token operator">=</span> <span class="token keyword">await</span> User<span class="token punctuation">.</span><span class="token function">create</span><span class="token punctuation">(</span><span class="token punctuation">{</span>
  <span class="token literal-property property">name</span><span class="token operator">:</span> <span class="token string">&quot;142vip.cn&quot;</span>
<span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token comment">// 写入成功后，可以通过user对象，获取主键ID</span>
console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span><span class="token string">&quot;userId:&quot;</span><span class="token punctuation">,</span> user<span class="token punctuation">.</span>id<span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre></div><p>可以检查数据库表中是否成功写入数据</p><h2 id="原始查询" tabindex="-1"><a class="header-anchor" href="#原始查询" aria-hidden="true">#</a> 原始查询</h2><p><code>Sequelize</code>框架本身是ORM框架，是用来管理对象实体和数据库表的关系映射，满足基础的<code>SQL</code>语句执行功能，同时还封装了大量查询操作的相关API方法。</p><p>这里主要介绍下如何用<code>Sequelize</code>框架执行原始<code>SQL</code>查询，简单来说就是通过写SQL来处理业务，执行<code>SQL</code>来实现业务功能。<code>sequelize</code> 框架提供的了<code>query()</code>方法来实现这一功能，查看源码：</p><figure><img src="`+i+`" alt="Sequelize的query()方法" tabindex="0" loading="lazy"><figcaption>Sequelize的query()方法</figcaption></figure><p>默认情况下，query()方法返回两个参数：</p><ul><li>结果数组</li><li>包含元数据的对象，例如：受影响的行数等</li></ul><p>针对不同的数据库类型（这个可以在建立Sequelize连接时通过dialect字段指定），原始的SQL语句会存在一些语法上的出入。</p><h3 id="简单查询" tabindex="-1"><a class="header-anchor" href="#简单查询" aria-hidden="true">#</a> 简单查询</h3><p>基于Sequelize.query()方法查询，可以满足一些基础的SQL语句执行。例如：</p><div class="language-javascript" data-ext="js"><pre class="language-javascript"><code><span class="token comment">// 将age等于18的数据对应的age字段改为30</span>
<span class="token keyword">const</span> sql <span class="token operator">=</span> <span class="token string">&quot;UPDATE users SET age = 30 WHERE age = 18&quot;</span>

<span class="token comment">// 执行SQL</span>
<span class="token keyword">const</span> <span class="token punctuation">[</span>results<span class="token punctuation">,</span> metadata<span class="token punctuation">]</span> <span class="token operator">=</span> <span class="token keyword">await</span> sequelize<span class="token punctuation">.</span><span class="token function">query</span><span class="token punctuation">(</span>sql<span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre></div><p>上面使用<code>results</code>字段来解构<code>SQL</code>语句执行后的结果，因为<code>SQL</code>语句是执行更新操作，所以<code>results</code>字段的值将会是一个<code>空数组</code>。</p><h3 id="格式化结果" tabindex="-1"><a class="header-anchor" href="#格式化结果" aria-hidden="true">#</a> 格式化结果</h3><p>根据源码可以知道<code>sequelize.query()</code>在执行<code>SQL</code>时，还可以传入第二个可选参数<code>options</code>对象，可以支持对<code>SQL</code>语句结果格式化的配置， 只需要配置<code>options.type</code>参数，例如：</p><div class="language-javascript" data-ext="js"><pre class="language-javascript"><code><span class="token keyword">const</span> <span class="token punctuation">{</span>QueryTypes<span class="token punctuation">}</span> <span class="token operator">=</span> <span class="token function">require</span><span class="token punctuation">(</span><span class="token string">&#39;sequelize&#39;</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token keyword">const</span> sql <span class="token operator">=</span> <span class="token string">&#39;select * from users&#39;</span><span class="token punctuation">;</span>
<span class="token comment">// 配置执行query方法的返回结果类型</span>
<span class="token keyword">const</span> users <span class="token operator">=</span> <span class="token keyword">await</span> sequelize<span class="token punctuation">.</span><span class="token function">query</span><span class="token punctuation">(</span>sql<span class="token punctuation">,</span> <span class="token punctuation">{</span>
  <span class="token literal-property property">type</span><span class="token operator">:</span> QueryTypes<span class="token punctuation">.</span><span class="token constant">SELECT</span>
<span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre></div><p><code>type</code>字段用来配置查询类型。查询类型会影响结果在传回之前的格式设置方式，type字段的配置支持多种类型，查看源码：</p><figure><img src="`+k+`" alt="query()方法的queryType支持的类型" tabindex="0" loading="lazy"><figcaption>query()方法的queryType支持的类型</figcaption></figure><h3 id="配置模型" tabindex="-1"><a class="header-anchor" href="#配置模型" aria-hidden="true">#</a> 配置模型</h3><p>对于执行查询<code>SQL</code>结果的处理，除了通过<code>type</code>字段去配置格式化，<code>Sequelize</code>还提供了将查询结果与指定模型的数据映射配置，例如：</p><div class="language-javascript" data-ext="js"><pre class="language-javascript"><code><span class="token keyword">const</span> <span class="token punctuation">{</span>QueryTypes<span class="token punctuation">}</span> <span class="token operator">=</span> <span class="token function">require</span><span class="token punctuation">(</span><span class="token string">&#39;sequelize&#39;</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token keyword">const</span> sql <span class="token operator">=</span> <span class="token string">&#39;select * from users&#39;</span><span class="token punctuation">;</span>
<span class="token comment">// 配置Users模型，将查询结果与模型进行数据映射</span>
<span class="token keyword">const</span> projects <span class="token operator">=</span> <span class="token keyword">await</span> sequelize<span class="token punctuation">.</span><span class="token function">query</span><span class="token punctuation">(</span>sql<span class="token punctuation">,</span> <span class="token punctuation">{</span>
  <span class="token literal-property property">model</span><span class="token operator">:</span> Users<span class="token punctuation">,</span>
  <span class="token literal-property property">mapToModel</span><span class="token operator">:</span> <span class="token boolean">true</span>
<span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre></div>`,23),h=s("code",null,"query()",-1),v={href:"https://github.com/sequelize/sequelize/blob/main/packages/core/src/dialects/abstract/query-interface.d.ts",target:"_blank",rel:"noopener noreferrer"},q=a(`<h3 id="替换参数" tabindex="-1"><a class="header-anchor" href="#替换参数" aria-hidden="true">#</a> 替换参数</h3><p>查询中的替换可以通过两种不同的方式完成，</p><ul><li>如果传递的是数组，<code>?</code> 将按照它们在数组中出现的顺序进行替换</li><li>如果传递了一个对象，<code>:key</code>则将替换为该对象中的键。如果对象中的键与key字段不匹配、找不到，会导致异常。</li></ul><p>这里通过简单的替换例子来演示，例如：</p><div class="language-javascript" data-ext="js"><pre class="language-javascript"><code><span class="token keyword">const</span> <span class="token punctuation">{</span>QueryTypes<span class="token punctuation">}</span> <span class="token operator">=</span> <span class="token function">require</span><span class="token punctuation">(</span><span class="token string">&#39;sequelize&#39;</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token comment">// 使用 ? 格式来替换，替换值为数组 </span>
<span class="token keyword">await</span> sequelize<span class="token punctuation">.</span><span class="token function">query</span><span class="token punctuation">(</span><span class="token string">&#39;SELECT * FROM tbl_user WHERE name = ?&#39;</span><span class="token punctuation">,</span> <span class="token punctuation">{</span>
  <span class="token literal-property property">replacements</span><span class="token operator">:</span> <span class="token punctuation">[</span>
    <span class="token string">&#39;142vip.cn&#39;</span>
  <span class="token punctuation">]</span><span class="token punctuation">,</span>
  <span class="token literal-property property">type</span><span class="token operator">:</span> QueryTypes<span class="token punctuation">.</span><span class="token constant">SELECT</span>
<span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token comment">// 使用:key 格式来替换，替换值为对象</span>
<span class="token keyword">await</span> sequelize<span class="token punctuation">.</span><span class="token function">query</span><span class="token punctuation">(</span><span class="token string">&#39;SELECT * FROM tbl_user WHERE name = :name&#39;</span><span class="token punctuation">,</span> <span class="token punctuation">{</span>
  <span class="token literal-property property">replacements</span><span class="token operator">:</span> <span class="token punctuation">{</span>
    <span class="token literal-property property">name</span><span class="token operator">:</span> <span class="token string">&#39;142vip.cn&#39;</span>
  <span class="token punctuation">}</span><span class="token punctuation">,</span>
  <span class="token literal-property property">type</span><span class="token operator">:</span> QueryTypes<span class="token punctuation">.</span><span class="token constant">SELECT</span>
<span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre></div><p>基于数组或者对象的形式实现查询SQL语句替换，类似的可以使用<code>%</code>来支持模糊匹配，例如：</p><div class="language-javascript" data-ext="js"><pre class="language-javascript"><code><span class="token keyword">const</span> <span class="token punctuation">{</span>QueryTypes<span class="token punctuation">}</span> <span class="token operator">=</span> <span class="token function">require</span><span class="token punctuation">(</span><span class="token string">&#39;sequelize&#39;</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token comment">// 使用:key 格式来替换，替换值为对象</span>
<span class="token keyword">await</span> sequelize<span class="token punctuation">.</span><span class="token function">query</span><span class="token punctuation">(</span><span class="token string">&#39;SELECT * FROM tbl_user WHERE name like :name&#39;</span><span class="token punctuation">,</span> <span class="token punctuation">{</span>
  <span class="token literal-property property">replacements</span><span class="token operator">:</span> <span class="token punctuation">{</span>
    <span class="token literal-property property">name</span><span class="token operator">:</span> <span class="token string">&#39;142vip.cn%&#39;</span>
  <span class="token punctuation">}</span><span class="token punctuation">,</span>
  <span class="token literal-property property">type</span><span class="token operator">:</span> QueryTypes<span class="token punctuation">.</span><span class="token constant">SELECT</span>
<span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre></div><h3 id="绑定参数" tabindex="-1"><a class="header-anchor" href="#绑定参数" aria-hidden="true">#</a> 绑定参数</h3><p>执行原始<code>SQL</code>语句，绑定参数和替换参数类型，不过绑定参数使用的是<code>$</code>标记，规则是：</p><ul><li>如果传递数组，<code>$1</code> 则绑定到数组中的第一个元素，数组角标为0</li><li>如果传递一个对象，<code>$key</code> 则绑定到<code>object[&#39;key&#39;]</code>。<strong>每个键必须以非数字字符开头、满足对象的键规范。</strong></li><li>在任何一种情况下<code>$$</code>都可以用来转义文字<code>$</code>符号。</li></ul><p>数组或对象必须包含所有绑定值，绑定参数与待绑定的 <code>SQL</code> 语句必须一一匹配，避免 <code>Sequelize</code> 因为绑定参数匹配问题出现异常。例如：</p><div class="language-javascript" data-ext="js"><pre class="language-javascript"><code><span class="token keyword">const</span> <span class="token punctuation">{</span>QueryTypes<span class="token punctuation">}</span> <span class="token operator">=</span> <span class="token function">require</span><span class="token punctuation">(</span><span class="token string">&#39;sequelize&#39;</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token comment">// 使用数组形式实现绑定参数</span>
<span class="token keyword">await</span> sequelize<span class="token punctuation">.</span><span class="token function">query</span><span class="token punctuation">(</span><span class="token string">&#39;SELECT * FROM tbl_user WHERE name = $1&#39;</span><span class="token punctuation">,</span> <span class="token punctuation">{</span>
  <span class="token literal-property property">bind</span><span class="token operator">:</span> <span class="token punctuation">[</span>
    <span class="token string">&#39;142vip.cn&#39;</span>
  <span class="token punctuation">]</span><span class="token punctuation">,</span>
  <span class="token literal-property property">type</span><span class="token operator">:</span> QueryTypes<span class="token punctuation">.</span><span class="token constant">SELECT</span>
<span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token comment">// 使用对象形式实现绑定参数</span>
<span class="token keyword">await</span> sequelize<span class="token punctuation">.</span><span class="token function">query</span><span class="token punctuation">(</span><span class="token string">&#39;SELECT * FROM tbl_user WHERE name = $name&#39;</span><span class="token punctuation">,</span> <span class="token punctuation">{</span>
  <span class="token literal-property property">bind</span><span class="token operator">:</span> <span class="token punctuation">{</span>
    <span class="token literal-property property">name</span><span class="token operator">:</span> <span class="token string">&#39;142vip.cn&#39;</span>
  <span class="token punctuation">}</span><span class="token punctuation">,</span>
  <span class="token literal-property property">type</span><span class="token operator">:</span> QueryTypes<span class="token punctuation">.</span><span class="token constant">SELECT</span>
<span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre></div><p>同样地，在绑定参数中也是支持模糊匹配的，使用好 <code>%</code> 标记即可</p><h3 id="特殊列处理" tabindex="-1"><a class="header-anchor" href="#特殊列处理" aria-hidden="true">#</a> 特殊列处理</h3><p>对于表的列字段带点，虽然从规范上不推荐这么做，但实际总是会有这种可能。例如：<code>user.name</code></p><div class="language-javascript" data-ext="js"><pre class="language-javascript"><code><span class="token keyword">const</span> <span class="token punctuation">{</span>QueryTypes<span class="token punctuation">}</span> <span class="token operator">=</span> <span class="token function">require</span><span class="token punctuation">(</span><span class="token string">&#39;sequelize&#39;</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token keyword">const</span> records <span class="token operator">=</span> <span class="token keyword">await</span> sequelize<span class="token punctuation">.</span><span class="token function">query</span><span class="token punctuation">(</span><span class="token string">&#39;select 1 as \`user.name\`&#39;</span><span class="token punctuation">,</span> <span class="token punctuation">{</span>
  <span class="token literal-property property">type</span><span class="token operator">:</span> QueryTypes<span class="token punctuation">.</span><span class="token constant">SELECT</span>
<span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span><span class="token constant">JSON</span><span class="token punctuation">.</span><span class="token function">stringify</span><span class="token punctuation">(</span>records<span class="token punctuation">[</span><span class="token number">0</span><span class="token punctuation">]</span><span class="token punctuation">,</span> <span class="token keyword">null</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre></div><p>查询出来的结果是：</p><div class="language-json5" data-ext="json5"><pre class="language-json5"><code><span class="token comment">// 查询结果按照列名整理成对象的形式</span>
<span class="token punctuation">{</span>
  <span class="token property">&quot;user.name&quot;</span><span class="token operator">:</span> <span class="token number">1</span>
<span class="token punctuation">}</span>
</code></pre></div><p>Sequelize框架可以使用 <code>nest</code>参数设置，处理这种特殊的列。当设置 <code>nest: true</code>时， 可以将生成的对象转化为嵌套对象，例如：</p><div class="language-javascript" data-ext="js"><pre class="language-javascript"><code><span class="token keyword">const</span> <span class="token punctuation">{</span>QueryTypes<span class="token punctuation">}</span> <span class="token operator">=</span> <span class="token function">require</span><span class="token punctuation">(</span><span class="token string">&#39;sequelize&#39;</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token keyword">const</span> records <span class="token operator">=</span> <span class="token keyword">await</span> sequelize<span class="token punctuation">.</span><span class="token function">query</span><span class="token punctuation">(</span><span class="token string">&#39;select 1 as \`user.name\`&#39;</span><span class="token punctuation">,</span> <span class="token punctuation">{</span>
  <span class="token literal-property property">type</span><span class="token operator">:</span> QueryTypes<span class="token punctuation">.</span><span class="token constant">SELECT</span>
<span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span><span class="token constant">JSON</span><span class="token punctuation">.</span><span class="token function">stringify</span><span class="token punctuation">(</span>records<span class="token punctuation">[</span><span class="token number">0</span><span class="token punctuation">]</span><span class="token punctuation">,</span> <span class="token keyword">null</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre></div><p>配置<code>nest</code>参数后，查询出来的结果是：</p><div class="language-json5" data-ext="json5"><pre class="language-json5"><code><span class="token comment">// 支持将查询的数据转化为嵌套对象</span>
<span class="token punctuation">{</span>
  <span class="token property">&quot;user&quot;</span><span class="token operator">:</span> <span class="token punctuation">{</span>
    <span class="token property">&quot;name&quot;</span><span class="token operator">:</span> <span class="token number">1</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div><h2 id="模型查询" tabindex="-1"><a class="header-anchor" href="#模型查询" aria-hidden="true">#</a> 模型查询</h2><p><code>Sequelize</code>框架封装了很多<code>API</code>来支持查询，避免反复写<code>SQL</code>语句，这些<code>API</code>本质上是将<code>SQL</code>语句抽象化， 用户只需要按照<code>API</code>提供的参数进行配置，即可实现和执行原始<code>SQL</code>语句一样的功能，例如：</p><p>提供了<code>findAll()</code>方法，支持在模型上使用，用来查询所有。</p><div class="language-javascript" data-ext="js"><pre class="language-javascript"><code><span class="token comment">// 查询用户表中的所有用户信息</span>
<span class="token keyword">const</span> users <span class="token operator">=</span> <span class="token keyword">await</span> User<span class="token punctuation">.</span><span class="token function">findAll</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token comment">// 判断查询结果是否为User模型的类对象</span>
console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span>users<span class="token punctuation">.</span><span class="token function">every</span><span class="token punctuation">(</span><span class="token parameter">user</span> <span class="token operator">=&gt;</span> user <span class="token keyword">instanceof</span> <span class="token class-name">User</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// true</span>
</code></pre></div><p>执行上面的<code>findAll()</code>函数，实质上就是执行转化后的<code>SQL</code>语句：</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token comment"># 查询用户表中的所有列</span>
<span class="token keyword">select</span> * from tbl_user
</code></pre></div><p>查看<code>findAll()</code>方法的源码：</p><figure><img src="`+d+`" alt="findAll()方法" tabindex="0" loading="lazy"><figcaption>findAll()方法</figcaption></figure><p>这里的可选参数<code>options</code>支持配置，例如：</p><ul><li><code>include</code>： 支持连表查询</li><li><code>order</code>： 支持查询结果排序</li><li><code>group</code>： 支持分组查询</li><li><code>limit</code>： 支持分页查询，基于不同角标起始值查询</li><li><code>where</code>： 支持条件查询</li><li>...</li></ul><h3 id="指定查询列" tabindex="-1"><a class="header-anchor" href="#指定查询列" aria-hidden="true">#</a> 指定查询列</h3><p>不传递参数，使用 <code>findAll()</code> 方法时，会默认查询表中的所有列，如果需要查询部分列，可以使用 <code>options</code> 中的 <code>attributes</code> 参数，例如：</p><div class="language-javascript" data-ext="js"><pre class="language-javascript"><code><span class="token comment">// 查询用户表中的\`name\`列信息</span>
<span class="token keyword">const</span> user2 <span class="token operator">=</span> User<span class="token punctuation">.</span><span class="token function">findAll</span><span class="token punctuation">(</span><span class="token punctuation">{</span>
  <span class="token literal-property property">attributes</span><span class="token operator">:</span> <span class="token punctuation">[</span><span class="token string">&#39;name&#39;</span><span class="token punctuation">]</span>
<span class="token punctuation">}</span><span class="token punctuation">)</span>

<span class="token comment">// 实质上是执行： select name from user</span>
</code></pre></div><p><code>attributes</code> 参数也支持对列查询时列属性重命名，类似于 <code>as</code> 的操作，例如：</p><div class="language-javascript" data-ext="js"><pre class="language-javascript"><code><span class="token comment">// 查询用户表中的\`name\`列信息</span>
<span class="token keyword">const</span> user2 <span class="token operator">=</span> User<span class="token punctuation">.</span><span class="token function">findAll</span><span class="token punctuation">(</span><span class="token punctuation">{</span>
  <span class="token literal-property property">attributes</span><span class="token operator">:</span> <span class="token punctuation">[</span><span class="token string">&#39;name&#39;</span><span class="token punctuation">,</span> <span class="token punctuation">[</span><span class="token string">&#39;age&#39;</span><span class="token punctuation">,</span> <span class="token string">&#39;newAge&#39;</span><span class="token punctuation">]</span><span class="token punctuation">]</span>
<span class="token punctuation">}</span><span class="token punctuation">)</span>

<span class="token comment">// 实质上是执行： select name , age as newAge from user</span>
</code></pre></div><p>查询<code>name</code>、<code>age</code>列，并且将 <code>age</code> 列重命名为 <code>newAge</code> 进行结果输出。</p><h3 id="包含某些列" tabindex="-1"><a class="header-anchor" href="#包含某些列" aria-hidden="true">#</a> 包含某些列</h3><p>除了查询表固定的数据列，还可以基于表的数据做一些操作，生成新的数据列，可以使用 <code>attributes</code> 参数中的 <code>include</code>关键字。例如：</p><p>只查询出user表中的name列</p><div class="language-javascript" data-ext="js"><pre class="language-javascript"><code><span class="token comment">// 只查询出name列</span>
<span class="token keyword">const</span> user4 <span class="token operator">=</span> User<span class="token punctuation">.</span><span class="token function">findAll</span><span class="token punctuation">(</span><span class="token punctuation">{</span>
  <span class="token literal-property property">attributes</span><span class="token operator">:</span> <span class="token punctuation">{</span>
    <span class="token literal-property property">include</span><span class="token operator">:</span> <span class="token punctuation">[</span><span class="token string">&#39;name&#39;</span><span class="token punctuation">]</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div><p>等价于：</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token keyword">select</span> name from tbl_user<span class="token punctuation">;</span>
</code></pre></div><p>也可以额外在增加列</p><div class="language-javascript" data-ext="js"><pre class="language-javascript"><code><span class="token comment">// 除了查询user表所有列外，还新增一列countAge</span>
<span class="token keyword">const</span> user5 <span class="token operator">=</span> User<span class="token punctuation">.</span><span class="token function">findAll</span><span class="token punctuation">(</span><span class="token punctuation">{</span>
  <span class="token literal-property property">attributes</span><span class="token operator">:</span> <span class="token punctuation">{</span>
    <span class="token literal-property property">include</span><span class="token operator">:</span> <span class="token punctuation">[</span>sequelize<span class="token punctuation">.</span><span class="token function">fn</span><span class="token punctuation">(</span><span class="token string">&#39;count&#39;</span><span class="token punctuation">,</span> sequelize<span class="token punctuation">.</span><span class="token function">col</span><span class="token punctuation">(</span><span class="token string">&#39;age&#39;</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">,</span> <span class="token string">&#39;countAge&#39;</span><span class="token punctuation">]</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span><span class="token punctuation">)</span>
</code></pre></div><p>等价于：</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token keyword">select</span> * , count<span class="token punctuation">(</span>age<span class="token punctuation">)</span> as <span class="token string">&quot;countAge&quot;</span> from tbl_user<span class="token punctuation">;</span>
</code></pre></div><p>灵活应用 <code>include</code> 配置和 <code>sequelize.fn()</code> 函数，可以一定程度上减少SQL原生语句的编写。</p><h3 id="排除某些列" tabindex="-1"><a class="header-anchor" href="#排除某些列" aria-hidden="true">#</a> 排除某些列</h3><p>和 <code>include</code> 配置支持的功能相反，<code>attributes</code> 参数中存在 <code>exclude</code> 配置，用来排除某些列，用法和include配置类似，例如：</p><div class="language-javascript" data-ext="js"><pre class="language-javascript"><code><span class="token comment">// 排除name数据列</span>
<span class="token keyword">const</span> user6 <span class="token operator">=</span> User<span class="token punctuation">.</span><span class="token function">findAll</span><span class="token punctuation">(</span><span class="token punctuation">{</span>
  <span class="token literal-property property">attributes</span><span class="token operator">:</span> <span class="token punctuation">{</span>
    <span class="token literal-property property">exclude</span><span class="token operator">:</span> <span class="token punctuation">[</span><span class="token string">&#39;name&#39;</span><span class="token punctuation">]</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span><span class="token punctuation">)</span>
</code></pre></div><p>转化SQL语句等价于：</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token comment"># 查询tbl_user表中非name字段的数据列</span>
<span class="token keyword">select</span> age from tbl_user<span class="token punctuation">;</span>
</code></pre></div><h3 id="列聚合运算" tabindex="-1"><a class="header-anchor" href="#列聚合运算" aria-hidden="true">#</a> 列聚合运算</h3><p>在查询表信息时，如果要对某列做一些操作，可以使用sequelize.fn函数来实现，例如：</p><div class="language-javascript" data-ext="js"><pre class="language-javascript"><code><span class="token comment">// 对name列实现count聚合操作，并取别名countAge</span>
<span class="token keyword">const</span> user3 <span class="token operator">=</span> User<span class="token punctuation">.</span><span class="token function">findAll</span><span class="token punctuation">(</span><span class="token punctuation">{</span>
  <span class="token literal-property property">attributes</span><span class="token operator">:</span> <span class="token punctuation">[</span>
    <span class="token string">&#39;name&#39;</span><span class="token punctuation">,</span>
    <span class="token punctuation">[</span>sequelize<span class="token punctuation">.</span><span class="token function">fn</span><span class="token punctuation">(</span><span class="token string">&#39;count&#39;</span><span class="token punctuation">,</span> sequelize<span class="token punctuation">.</span><span class="token function">col</span><span class="token punctuation">(</span><span class="token string">&#39;age&#39;</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">,</span> <span class="token string">&#39;countAge&#39;</span><span class="token punctuation">]</span>
  <span class="token punctuation">]</span>
<span class="token punctuation">}</span><span class="token punctuation">)</span>
</code></pre></div><p>使用 <code>sequelize.fn()</code> 函数实现聚合操作，将结果放在 <code>attributes</code> 参数中，可以用来生成新的列</p><h3 id="条件查询" tabindex="-1"><a class="header-anchor" href="#条件查询" aria-hidden="true">#</a> 条件查询</h3><p>在<code>findAll()</code>方法的API中，使用关键字<code>where</code>支持条件查询、筛选数据，例如：</p><div class="language-javascript" data-ext="js"><pre class="language-javascript"><code><span class="token keyword">const</span> user7 <span class="token operator">=</span> User<span class="token punctuation">.</span><span class="token function">findAll</span><span class="token punctuation">(</span><span class="token punctuation">{</span>
  <span class="token literal-property property">where</span><span class="token operator">:</span> <span class="token punctuation">{</span>
    <span class="token literal-property property">name</span><span class="token operator">:</span> <span class="token string">&#39;142vip.cn&#39;</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span><span class="token punctuation">)</span>
</code></pre></div><p>转换为SQL语句等价于：</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token keyword">select</span> * from tbl_user where <span class="token assign-left variable">name</span><span class="token operator">=</span><span class="token string">&#39;142vip.cn&#39;</span>
</code></pre></div><p>除了支持上面这种相等的比较，<code>findAll()</code>方法还依赖<code>Op</code>支持大于、小于等比较，例如：</p><div class="language-javascript" data-ext="js"><pre class="language-javascript"><code><span class="token keyword">const</span> <span class="token punctuation">{</span>Op<span class="token punctuation">}</span> <span class="token operator">=</span> <span class="token function">require</span><span class="token punctuation">(</span><span class="token string">&quot;sequelize&quot;</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token comment">// 等价于上面的操作</span>
<span class="token keyword">const</span> user7 <span class="token operator">=</span> User<span class="token punctuation">.</span><span class="token function">findAll</span><span class="token punctuation">(</span><span class="token punctuation">{</span>
  <span class="token literal-property property">where</span><span class="token operator">:</span> <span class="token punctuation">{</span>
    <span class="token literal-property property">name</span><span class="token operator">:</span> <span class="token punctuation">{</span>
      <span class="token punctuation">[</span>Op<span class="token punctuation">.</span>eq<span class="token punctuation">]</span><span class="token operator">:</span> <span class="token string">&quot;142vip.cn&quot;</span>
    <span class="token punctuation">}</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span><span class="token punctuation">)</span>
</code></pre></div><p><code>Sequelize</code> 默认情况支持<code>相等比较</code>，也就是上面基于<code>Op</code>的实现等价与<code>Sequelize</code>的相等比较。</p>`,66),w=s("code",null,"Op.eq",-1),b=s("code",null,"Sequelize",-1),j=s("code",null,"Op",-1),x=a(`<h2 id="查找器查询" tabindex="-1"><a class="header-anchor" href="#查找器查询" aria-hidden="true">#</a> 查找器查询</h2><p>在数据库的查询语句中，一般使用<code>SELECT</code>关键字。但对于ORM框架来说，可以封装出很多不同用法的<code>API</code>方法，来实现查询操作， <code>Sequelize</code>框架提供了很多查找器方法，例如：<code>findAll</code> 、<code>findByPk</code>、<code>findOne</code>...</p><p><strong>在默认情况下，所有查找器方法的结果都是模型类的实例，不是简单的JS对象</strong></p><p>执行查找器方法查询数据库，返回数据时，<code>Sequelize</code>框架会自动将返回的内容包装在对应的模型实例上， 但是数据量过大时，这种数据的转换需要消耗大量的时间，可以设置<code>raw: true</code>配置来关闭这种转换，返回原始的JS对象数据。</p><h3 id="findall方法" tabindex="-1"><a class="header-anchor" href="#findall方法" aria-hidden="true">#</a> findAll方法</h3><p>在上面已经提供过很多关于findAll()方法的使用，本质上是生成一个标准的<code>SELECT</code>查询，例如：</p><div class="language-javascript" data-ext="js"><pre class="language-javascript"><code><span class="token keyword">const</span> <span class="token punctuation">{</span>Op<span class="token punctuation">}</span> <span class="token operator">=</span> <span class="token function">require</span><span class="token punctuation">(</span><span class="token string">&quot;sequelize&quot;</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token comment">// 查询满足条件的所有用户</span>
User<span class="token punctuation">.</span><span class="token function">findAll</span><span class="token punctuation">(</span><span class="token punctuation">{</span>
  <span class="token comment">// 返回原生对象结果</span>
  <span class="token literal-property property">raw</span><span class="token operator">:</span> <span class="token boolean">true</span><span class="token punctuation">,</span>
  <span class="token comment">// 条件查询</span>
  <span class="token literal-property property">where</span><span class="token operator">:</span> <span class="token punctuation">{</span>
    <span class="token literal-property property">name</span><span class="token operator">:</span> <span class="token punctuation">{</span>
      <span class="token punctuation">[</span>Op<span class="token punctuation">.</span>eq<span class="token punctuation">]</span><span class="token operator">:</span> <span class="token string">&quot;142vip.cn&quot;</span>
    <span class="token punctuation">}</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span><span class="token punctuation">)</span>
</code></pre></div><h3 id="findbypk方法" tabindex="-1"><a class="header-anchor" href="#findbypk方法" aria-hidden="true">#</a> findByPk方法</h3><p><code>findByPk()</code>方法是根据主键内容查询查询单条数据，因为主键具有唯一性，例如：</p><div class="language-javascript" data-ext="js"><pre class="language-javascript"><code><span class="token keyword">const</span> user <span class="token operator">=</span> <span class="token keyword">await</span> User<span class="token punctuation">.</span><span class="token function">findByPk</span><span class="token punctuation">(</span><span class="token number">123</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token keyword">if</span> <span class="token punctuation">(</span>user <span class="token operator">===</span> <span class="token keyword">null</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
  console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span><span class="token string">&#39;Not found!&#39;</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span> <span class="token keyword">else</span> <span class="token punctuation">{</span>
  console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span>project <span class="token keyword">instanceof</span> <span class="token class-name">Project</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// true</span>
<span class="token punctuation">}</span>
</code></pre></div><p>在上面的查询中，因为没有设置<code>raw</code>参数配置，默认情况下，将查询结果转为为<code>Sequelize</code>框架的模型实例对象</p><h3 id="findone方法" tabindex="-1"><a class="header-anchor" href="#findone方法" aria-hidden="true">#</a> findOne方法</h3><p><code>findOne()</code>方法是用来查询满足条件的第一条数据，如果没有提供查询条件，则默认返回表中第一条数据，例如：</p><div class="language-javascript" data-ext="js"><pre class="language-javascript"><code><span class="token comment">// 查询满足条件的第一条数据</span>
User<span class="token punctuation">.</span><span class="token function">findOne</span><span class="token punctuation">(</span><span class="token punctuation">{</span>
  <span class="token literal-property property">where</span><span class="token operator">:</span> <span class="token punctuation">{</span>
    <span class="token literal-property property">name</span><span class="token operator">:</span> <span class="token string">&quot;142vip.cn&quot;</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span><span class="token punctuation">)</span>
</code></pre></div><h3 id="findorcreate方法" tabindex="-1"><a class="header-anchor" href="#findorcreate方法" aria-hidden="true">#</a> findOrCreate方法</h3><p><code>findOrCreate()</code> 方法是在数据库表中查询或者创建数据，存在两种情况：</p><ul><li>表中存在满足查询条件的数据，则直接返回</li><li>表中不存在满足条件的数据，则根据查询条件和默认值在表中新创建一条数据</li></ul><p>查看 <code>findOrCreate()</code> 方法的源码：</p><figure><img src="`+g+`" alt="findOrCreate()方法的定义" tabindex="0" loading="lazy"><figcaption>findOrCreate()方法的定义</figcaption></figure><p><code>findOrCreate()</code>方法返回一个二元数组，形式为：</p><ul><li>&lt;<code>Model</code>，<code>Boolean</code>&gt;[<code>查询结果集</code> , <code>是否新插入</code>]</li></ul><p>可以根据布尔值结果来判断是否为新创建的数据， 例如：</p><div class="language-javascript" data-ext="js"><pre class="language-javascript"><code><span class="token comment">// 查询用户信息，如果不存在则创建用户，并且age字段默认值为18</span>
<span class="token keyword">const</span> <span class="token punctuation">[</span>user<span class="token punctuation">,</span> created<span class="token punctuation">]</span> <span class="token operator">=</span> <span class="token keyword">await</span> User<span class="token punctuation">.</span><span class="token function">findOrCreate</span><span class="token punctuation">(</span><span class="token punctuation">{</span>
  <span class="token literal-property property">where</span><span class="token operator">:</span> <span class="token punctuation">{</span><span class="token literal-property property">username</span><span class="token operator">:</span> <span class="token string">&#39;142vip.cn&#39;</span><span class="token punctuation">}</span><span class="token punctuation">,</span>
  <span class="token literal-property property">defaults</span><span class="token operator">:</span> <span class="token punctuation">{</span>
    <span class="token literal-property property">age</span><span class="token operator">:</span> <span class="token number">18</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token keyword">if</span> <span class="token punctuation">(</span>created<span class="token punctuation">)</span> <span class="token punctuation">{</span>
  console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span><span class="token string">&quot;新创建的数据信息&quot;</span><span class="token punctuation">)</span>
<span class="token punctuation">}</span>
</code></pre></div><h3 id="findandcountall方法" tabindex="-1"><a class="header-anchor" href="#findandcountall方法" aria-hidden="true">#</a> findAndCountAll方法</h3><p><code>findAndCountAll()</code>方法结合了find() 和 count() 方法，在处理分页这种业务时非常有用。查看源码：</p><figure><img src="`+y+`" alt="findAndCountAll()方法定义" tabindex="0" loading="lazy"><figcaption>findAndCountAll()方法定义</figcaption></figure><p>从类型声明上可以看出，不论查询参数中是否传入<code>group</code>参数，都会返回两个参数：</p><ul><li><code>count</code>： <ul><li>没有传入<code>group</code>参数进行分组查询时，返回一个整数，代表匹配的记录总数</li><li>传入<code>group</code>参数进行分组查询时，返回对象数组，代表每组中的计数和分组名</li></ul></li><li><code>rows</code>： 对象数组 ，包含满足条件的所有记录</li></ul><p>特别需要注意下，进行分组查询时，count返回结果的形式：</p><div class="language-typescript" data-ext="ts"><pre class="language-typescript"><code><span class="token keyword">export</span> <span class="token keyword">interface</span> <span class="token class-name">GroupedCountResultItem</span> <span class="token punctuation">{</span>
  <span class="token comment">// 分组的属性值</span>
  <span class="token punctuation">[</span>key<span class="token operator">:</span> <span class="token builtin">string</span><span class="token punctuation">]</span><span class="token operator">:</span> <span class="token builtin">unknown</span>

  <span class="token comment">// 总数</span>
  count<span class="token operator">:</span> <span class="token builtin">number</span>
<span class="token punctuation">}</span>
</code></pre></div><p>这里简单介绍下<code>findAndCountAll()</code>方法的使用，例如：</p><div class="language-javascript" data-ext="js"><pre class="language-javascript"><code><span class="token comment">// 分页查询</span>
<span class="token keyword">const</span> <span class="token punctuation">{</span>count<span class="token punctuation">,</span> rows<span class="token punctuation">}</span> <span class="token operator">=</span> <span class="token keyword">await</span> User<span class="token punctuation">.</span><span class="token function">findAndCountAll</span><span class="token punctuation">(</span><span class="token punctuation">{</span>
  <span class="token literal-property property">where</span><span class="token operator">:</span> <span class="token punctuation">{</span>
    <span class="token literal-property property">title</span><span class="token operator">:</span> <span class="token punctuation">{</span>
      <span class="token punctuation">[</span>Op<span class="token punctuation">.</span>like<span class="token punctuation">]</span><span class="token operator">:</span> <span class="token string">&#39;142vip&#39;</span>
    <span class="token punctuation">}</span>
  <span class="token punctuation">}</span><span class="token punctuation">,</span>
  <span class="token literal-property property">offset</span><span class="token operator">:</span> <span class="token number">10</span><span class="token punctuation">,</span>
  <span class="token literal-property property">limit</span><span class="token operator">:</span> <span class="token number">2</span>
<span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre></div><p>关于利用<code>indAndCountAll()</code>方法来实现分页逻辑，可以参数<a href="">最佳实践-分页查询</a></p><h2 id="子查询" tabindex="-1"><a class="header-anchor" href="#子查询" aria-hidden="true">#</a> 子查询</h2>`,34);function S(A,_){const e=p("ExternalLinkIcon"),o=p("RouterLink");return l(),u("div",null,[m,s("p",null,[n("更多使用"),h,n("方法的配置， 可以查看"),s("a",v,[n("QueryOptions的类型声明"),t(e)]),n(" ，提供了丰富的API方法。")]),q,s("p",null,[n("除了"),w,n("运算符 ，"),b,n("框架中的"),j,n("还提供了丰富的运算符，具体可以参考"),t(o,{to:"/manuscripts/server-end/orm/sequelize/tutorial/OP%E8%BF%90%E7%AE%97%E7%AC%A6.html"},{default:r(()=>[n("Op运算符")]),_:1})]),x])}const L=c(f,[["render",S],["__file","db-query.html.vue"]]);export{L as default};
