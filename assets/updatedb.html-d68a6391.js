import{_ as e}from"./plugin-vue_export-helper-c27b6911.js";import{o as d,c as o,e as c}from"./app-be253e4c.js";const a={},s=c(`<h1 id="updatedb命令" tabindex="-1"><a class="header-anchor" href="#updatedb命令" aria-hidden="true">#</a> updatedb命令</h1><p><code>updatedb</code>创建或更新<code>locate</code>命令使用的数据库，如果数据库已经存在，则重用其数据以避免重新读取未更改的目录，<code>updatedb</code>通常由<code>cron</code>每天运行以更新默认数据库。</p><h2 id="语法" tabindex="-1"><a class="header-anchor" href="#语法" aria-hidden="true">#</a> 语法</h2><div class="language-bash" data-ext="sh"><pre class="language-bash"><code>updatedb <span class="token punctuation">[</span>OPTION<span class="token punctuation">]</span><span class="token punctuation">..</span>.
</code></pre></div><h2 id="参数" tabindex="-1"><a class="header-anchor" href="#参数" aria-hidden="true">#</a> 参数</h2><ul><li><code>-f, --add-prunefs FS</code>: 将空格分隔的列表<code>FS</code>中的条目添加到<code>PRUNEFS</code>。</li><li><code>-n, --add-prunenames NAMES</code>: 将空格分隔的列表名称中的条目添加到<code>pruneName</code>。</li><li><code>-e, --add-prunepaths PATHS</code>: 将以空格分隔的列表<code>PATHS</code>中的条目添加到<code>PRUNEPATHS</code>。</li><li><code>-U, --database-root PATH</code>: 仅存储扫描以生成的数据库路径为根的文件系统子树的结果，默认情况下会扫描整个文件系统即<code>/</code>，<code>locate</code>输出的条目作为绝对路径名，不包含符号链接，无论<code>PATH</code>的形式如何。</li><li><code>-h, --help</code>: 显示帮助信息。</li><li><code>-o, --output FILE</code>: 将数据库写入文件，而不是使用默认数据库，数据库默认位置为<code>/var/lib/mlocate/mlocate.db</code>。</li><li><code>--prune-bind-mounts FLAG</code>: 将<code>PRUNE_BIND_MOUNTS</code>设置为<code>FLAG</code>，覆盖配置文件，默认为<code>no</code>。</li><li><code>--prunefs FS</code>: 将<code>PRUNEFS</code>设置为<code>FS</code>，以覆盖配置文件。</li><li><code>--prunenames NAMES</code>: 将<code>PRUNENAMES</code>设置为<code>NAMES</code>，以覆盖配置文件。</li><li><code>--prunepaths PATHS</code>: 将<code>PRUNEPATHS</code>设置为<code>PATHS</code>，以覆盖配置文件。</li><li><code>-l, --require-visibility FLAG</code>: 将生成的数据库中的&quot;报告之前要求文件可见性&quot;标志设置为<code>FLAG</code>，默认值为<code>yes</code>。如果<code>FLAG</code>为<code>0</code>或<code>no</code>，或者数据库文件可被<code>others</code>读取或不属于<code>slocate</code>，那么<code>locate</code>将输出数据库项，运行<code>locate</code>的用户无法读取所需的目录以找到数据库项所描述的文件。如果<code>FLAG</code>为<code>1</code>或<code>yes</code>，则<code>locate</code>在将每个条目的父目录报告给调用用户之前，先检查其父目录的权限。为了使文件的存在真正被其他用户隐藏，数据库组设置为<code>slocate</code>，并且数据库权限禁止用户使用<code>locate set-gid slocate</code>以外的其他方式读取用户的数据库。注意，仅当数据库由<code>slocate</code>拥有并且<code>others</code>不可读时，才检查可见性标志。</li><li><code>-v, --verbose</code>: 将文件的输出路径名输出到标准输出。</li><li><code>-V, --version</code>: 输出版本信息。</li></ul><h2 id="示例" tabindex="-1"><a class="header-anchor" href="#示例" aria-hidden="true">#</a> 示例</h2><p>更新<code>locate</code>命令所使用的数据库。</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code>updatedb
</code></pre></div><p>更新<code>locate</code>命令所使用的数据库，并输出找到的文件。</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code>updatedb <span class="token parameter variable">-v</span>
</code></pre></div><p>指定更新<code>locate</code>命令所使用的数据库的目录。</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code>updatedb <span class="token parameter variable">-U</span> /home
</code></pre></div>`,13),t=[s];function l(n,i){return d(),o("div",null,t)}const u=e(a,[["render",l],["__file","updatedb.html.vue"]]);export{u as default};
